'PunktDe.Rebirth:Document.RestoreTarget':
  superTypes:
    'Neos.Neos:Document': true

  ui:
    label: Restore Target
    icon: fas fa-recycle

  constraints:
    nodeTypes:
      '*': false

  properties:
    _hidden:
      defaultValue: true

    targetMode:
      type: string
      defaultValue: parentNode
      ui:
        inspector:
          group: null

    target:
      type: string
      ui:
        inspector:
          group: null
